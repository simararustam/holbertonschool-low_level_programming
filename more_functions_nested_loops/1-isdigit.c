#include "main.h"
/**
 * _isdigit - entry point
 * Description: This function prints the alphabet, in lowercase
 * @c: int variable
 * main - check the code
 * Return: Always 0
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
		return (0);

	return (0);
}
