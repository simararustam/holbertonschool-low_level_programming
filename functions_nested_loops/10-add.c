#include "main.h"
/**
 * add - entry point
 * Description: This function prints the alphabet, in lowercase
 * @x: entry parametr
 * @y: entry point
 * main - check the code
 * Return: Always 0
 */

int add(int x, int y)
{
	return (x + y);
}
